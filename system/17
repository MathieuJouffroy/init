# SUBJECT :
# Find the command that allows you to connect via ssh on the VM.
# (In parallel with the graphic session)

# VirtualBox -> create private network which will be connecter to your host networl
# using NAT
# therefore -> cannot directly access any host of the private network from the
# host network. VirtualBox 
# acces it through your localhost -> 127.0.0.1

# COMMAND :  ssh <user>@<ip-address-or-hostname>
ssh mjouffro@127.0.0.1 